---
import blue_summit from '../../assets/Baubuero_Blue-Summit_09.jpg';
import { getImage } from "astro:assets";
import buero4 from '../../assets/buero4.jpg';
import buero5 from '../../assets/buero5.jpg';
import buero6 from '../../assets/buero6.jpg';
import buero7 from '../../assets/buero7.jpg';

const optimizedBuero4 = await getImage({src: buero4, format: 'webp'});
const optimizedBuero5 = await getImage({src: buero5, format: 'webp'});
const optimizedBuero6 = await getImage({src: buero6, format: 'webp'});
const optimizedBuero7 = await getImage({src: buero7, format: 'webp'});

---

<div class="container">
    <h2>Ihre Wünsche<span>.</span></h2>
    <div class="scope-grid section dark-text">
        <div>
            <p>
                Auf uns können Sie bauen, denn Ihre Ziele setzen wir um!
            </p>
            <p>
                Ihre Wünsche und Vorstellungen für ein Projekt stehen für uns an erster Stelle.  Unsere Aufgabe ist es sie zu verstehen und umzusetzen.
            </p>
        </div>
        <img class="round-img" src={blue_summit.src} width="375" style="background-color: #eee;">
    </div>
    <div class="scope-info">
        <div class="round-img" style={`background-image: url(${optimizedBuero4.src});`}>
            <div>
                <h3>Beratung</h3>
                <p>immer im Sinne des Auftraggebers</p>
            </div>
            
        </div>
        <div class="round-img" style={`background-image: url(${optimizedBuero5.src});`}>
            <div>
                <h3>Qualität</h3>
                <p>in der Umsetzung der Projekte</p>
            </div>
        </div>
        <div class="round-img" style={`background-image: url(${optimizedBuero6.src});`}>
            <div>
                <h3>Fertigstellung</h3>
                <p>fristgerechte ohne Verzug</p>
            </div>
            
        </div>
        <div class="round-img" style={`background-image: url(${optimizedBuero7.src});`}>
            <div>
                <h3>Kommunikation</h3>
                <p>auf Augenhöhe mit allen Beteiligten</p>
            </div> 
        </div>
    </div>
</div>

<style>
    .scope-grid{
        display: grid;
        grid-template-columns: 1fr auto;
        gap: 50px;
    }
    .scope-info{
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 25px;
        margin: 15px 0 70px;
    }
    .scope-info > div{
        position: relative;
        display: flex;
        flex-wrap: nowrap;
        height: 220px;
        padding: 15px;
        background-size: cover!Important;
        justify-content: center;
        align-items: end;
        color: #fff;
        text-align: center;
        box-shadow: inset 0 -78px 60px -20px #212121;
        /* background: linear-gradient(to bottom, transparent 50%, rgba(0, 0, 0, 0.85) 100%); */ */
    }
    .scope-info h3{
        margin: 0;
        font-size: 24px;
    }
    .scope-info p{
        margin: 0;
        font-size: 16px;
    }

</style>